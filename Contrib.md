This project uses C++ (Arduino framework) for the ESP32 and Python for the server.

Workflow Steps:

Fork the repository (create their own copy).

Clone it to their local machine.

Create a new Branch (e.g., git checkout -b feature/better-logging).

Make changes and Commit.

Push to their fork.

Open a Pull Request (PR) to your main branch.
